#include "main.h"

/**
 * is_prime_number - A function that returns 1 if the input is an int,
 * and 0 otherwise
 * @n: Intager
 * Return: 0
 */

int is_prime_number(int n)
{
	if (n < 0)
		return (0);
	if (n > 1)
		return (1);
}
