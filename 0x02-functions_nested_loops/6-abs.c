#include "main.h"

/**
 * _abs - A function that computes the absolute value of an integer
 * @r: Value to be computed
 * Return: Absalute value of an intager
 */

int _abs(int r)
{

	if (r >= 0)
	{
		return (r);
	}
		return (-r);
}
